Here we got doubt about if the internal package has same name as the registry package what will happen so npm will always look for the internal package first then it will if it exists then it will resolve and install or else it will try to pull from the remote package

so comapnies usually follow @company name/package name

